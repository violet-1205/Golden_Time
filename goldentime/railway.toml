[build]
builder = "NIXPACKS"
buildCommand = "./gradlew bootJar -x test"

[deploy]
startCommand = "java -Dfile.encoding=UTF-8 -jar build/libs/goldentime-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
